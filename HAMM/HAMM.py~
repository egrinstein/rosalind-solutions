#!/usr/bin env python

dna_strings = sys.argv[1]
dna_strings = open(dna_strings, 'r')
dna_strings = dna_strings.readlines()

hamm = 0

for symbol in dna_strings[0]:
    if dna_strings[0] != dna_strings[1]:
        hamm += 1

print hamm
